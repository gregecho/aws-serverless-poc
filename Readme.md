# Install pnpm

```
npm install -g pnpm
```

# Create project

```
mkdir aws-serverless-demo
cd aws-serverless-demo
pnpm init
```

TODO:

- Standard error handler & validation error
- Onion architecture
- schema vs type?
- Full typed
